# TODO

- [x] Inspect existing chatbot + API client and confirm it posts to `/api/chat`
- [x] Inspect backend; confirm `/api/chat` exists in `Backend.py`
- [x] Fix frontend API base URL default to local backend (`http://localhost:5001/api`) when env var isn’t set
- [x] Improve `/api/chat` fallback/error handling in `Backend.py` so chatbot can respond even when Gemini isn’t configured

- [ ] Run backend + frontend and manually test chat with a few general questions

